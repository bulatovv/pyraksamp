[package]
name = "samp_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "samp_core"
crate-type = ["cdylib"]

[dependencies]
pyraksamp_core = { path = ".." }
pyo3 = { version = "0.23", features = ["extension-module", "abi3-py37"] }
